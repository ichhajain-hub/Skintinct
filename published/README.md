# published/

Meera's 15 original pieces (already folded into `voice-skill.md`) plus every
post the pipeline drafts that she later replies `approve` to — committed here
automatically by the webhook when that happens.

This folder is the growing voice reference. It's read-only from the
pipeline's side except for that one write path; nothing here is generated
without her explicit approval.

Each auto-committed file is named `YYYY-MM-DD-<draft-id>.md` with frontmatter
recording which note(s) it came from and when it was approved.
